<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liao.propertymanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.user_profile.ProfileActivity"> </activity>
        <activity android:name=".ui.tenants_management.TenantsAllActivity" />
        <activity android:name=".ui.auth.ForgetPasswordActivity" />
        <activity android:name=".ui.properties.PropertiesActivity" />
        <activity android:name=".ui.todoList.AddToDoListActivity" />
        <activity android:name=".ui.auth.SplashActivity">

        </activity>
        <activity android:name=".ui.home.HomeActivity" ><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".ui.auth.NavActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.LoginActivity" />
    </application>

</manifest>